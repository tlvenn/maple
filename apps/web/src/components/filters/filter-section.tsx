// Promoted to @maple/ui so the local-mode UI shares the exact same filter chrome.
export {
	FilterSection,
	SearchableFilterSection,
	SingleCheckboxFilter,
	serviceColorMap,
	type FilterOption,
} from "@maple/ui/components/filters/filter-section"
