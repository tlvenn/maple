/**
 * Content width of the alert-rule editor. The form body and the sticky action
 * bar are separate subtrees that must line up, so they share this one value
 * rather than each hard-coding a literal that can drift.
 */
export const RULE_FORM_MAX_WIDTH = "max-w-[1100px]"
