diff --git a/src/services/workers.ts b/src/services/workers.ts
index bc2401ef6453b0d46b75662896787ad9a6c641ad..cebf4f6ebb0ad5a54f63616cde30771f6dd04e09 100644
--- a/src/services/workers.ts
+++ b/src/services/workers.ts
@@ -1700,7 +1700,10 @@ const GetBetaWorkerVersionResponsePlacement4 =
   /*@__PURE__*/ Schema.suspend(() =>
     Schema.Struct({
       mode: Schema.Literal("targeted"),
-      region: Schema.String,
+      // Patched (maple): Cloudflare's put-script response can echo a targeted
+      // placement without the region (`{"mode":"targeted"}`), which failed this
+      // union and surfaced as CloudflareHttpError on a 200 response.
+      region: Schema.optional(Schema.String),
     }),
   ) as unknown as Schema.Codec<GetBetaWorkerVersionResponsePlacement4>;
 
@@ -5957,7 +5960,10 @@ const ListScriptsResponseResultPlacement4 =
   /*@__PURE__*/ Schema.suspend(() =>
     Schema.Struct({
       mode: Schema.Literal("targeted"),
-      region: Schema.String,
+      // Patched (maple): Cloudflare's put-script response can echo a targeted
+      // placement without the region (`{"mode":"targeted"}`), which failed this
+      // union and surfaced as CloudflareHttpError on a 200 response.
+      region: Schema.optional(Schema.String),
       lastAnalyzedAt: Schema.optional(
         Schema.Union([Schema.String, Schema.Null]),
       ),
